<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lx.dao.FollowMapper" >
  <resultMap id="BaseResultMap" type="com.lx.model.Follow" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="userid" property="userid" jdbcType="INTEGER" />
    <result column="follow_id" property="followId" jdbcType="INTEGER" />
  </resultMap>
  <insert id="insert" parameterType="com.lx.model.Follow" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into follow (userid, follow_id)
    values (#{userid,jdbcType=INTEGER}, #{followId,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.lx.model.Follow" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into follow
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="userid != null" >
        userid,
      </if>
      <if test="followId != null" >
        follow_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="userid != null" >
        #{userid,jdbcType=INTEGER},
      </if>
      <if test="followId != null" >
        #{followId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>

  <!--查询某用户的粉丝数量 select count(*) from follow where follow_id = 1-->
  <select id="fan_count" resultType="java.lang.Integer" parameterType="java.lang.Integer">
    select count(*) from follow where follow_id = #{follow_id,jdbcType=INTEGER}
  </select>


  <!-- 查询该用户的关注数量 select count(*) from follow where userid = 1 -->
  <select id="follow_count" resultType="java.lang.Integer" parameterType="java.lang.Integer">
    select count(*) from follow where userid = #{userid,jdbcType=INTEGER}
  </select>

  <!--关注功能 /* ID = userid 的用户 关注 ID = follow_id 的用户 */ -->
<insert id="follow_user" parameterType="java.lang.Integer">
  INSERT into follow(userid,follow_id) VALUES (#{userid,jdbcType=INTEGER},#{followId,jdbcType=INTEGER})
</insert>

<!--取消关注功能 /* ID = userid 的用户取消关注  ID = follow_id 的用户 */-->
  <delete id="cancel_follow" parameterType="java.lang.Integer">
    delete from follow where userid = #{userid,jdbcType=INTEGER} and  follow_id = #{followId,jdbcType=INTEGER}
  </delete>

  <!--查询 ID = userid 的用户是否已经关注 ID = follow_id 的用户 -->
  <select id="select_if_follow" parameterType="java.lang.Integer" resultType="java.lang.Integer">
    select count(*) from follow where userid = #{userid,jdbcType=INTEGER} and follow_id = #{followId,jdbcType=INTEGER}
  </select>

</mapper>